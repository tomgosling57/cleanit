#!/bin/bash
# View bash-shell for the specified container
if [ -z "$1" ]; then
  echo "Usage: $0 <container_name>"
  exit 1
fi

docker exec -it "cleanit-$1" /bin/bash
echo "CMD: docker exec -it cleanit-$1 /bin/bash"