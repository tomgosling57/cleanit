#!/bin/bash
# View logs for the specified container
if [ -z "$1" ]; then
  echo "Usage: $0 <container_name>"
  exit 1
fi

docker compose logs "$1" --tail=20
echo "CMD: docker compose logs $1 --tail=20"